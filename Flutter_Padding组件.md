# Flutter_Padding组件

```dart
class PaddingStu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.fromLTRB(10, 10, 0, 0),
      child: Container(
          //...
          ),
    );
  }
}
```
